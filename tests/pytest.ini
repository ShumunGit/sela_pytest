[pytest]
markers =
    smoke: mark a test as a Smoke Test.
    regression: mark a test as a regression Test.
    printTests: mark as test function with no return value.
log_format = %(asctime)s %(levelname)s %(message)s
log_date_format = %Y-%m-%d %H:%M:%S
log_cli = true
log_cli_level = INFO
